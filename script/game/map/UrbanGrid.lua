local UrbanGrid = class( "WorldMap.UrbanGrid" )

function UrbanGrid:init( width, height )
	for x = 1, width do
		local column = WorldMap.Line( height )
		for y = 1, height do
			local row = WorldMap.Line( width )
			

		end
	end
end
